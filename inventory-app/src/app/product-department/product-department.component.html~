<div class="product-department">
	@for (name of product.department; track name; let index = $index){
	<span>
		<a href="#">{{ name }}</a>
		<span>{{i < (product.department.length-1) ? '>' : ''}}</span>
	</span>
	}
</div>
